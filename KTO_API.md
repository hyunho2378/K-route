# KTO_API.md · 한국관광공사 OpenAPI 활용 계약 (근거 문서 채워지기 전 플레이스홀더)

> **경고: 이 파일의 엔드포인트·오퍼레이션명·파라미터는 아직 확정 아님.**
> docs/kto/ 에 공사 활용가이드(PDF/DOCX)를 배치한 뒤, 그 문서에 적힌 것만 사실로 채운다.
> 문서 없이 이름을 지어내면 tago 계약 위반 = 즉시 실패. 아래는 '어떤 서비스가 필요한지'의
> 요구사항 목록일 뿐, 실제 오퍼레이션명이 아니다.

## 발급 필요 (사용자 작업 · api.visitkorea.or.kr 활용신청)
1. 국문 관광정보 서비스(지역기반 목록·상세)
2. 다국어 관광정보 서비스(영어 최소 · 일/중 여유되면)
3. 관광지별 연관 관광지 정보
4. 관광지 집중률·방문자 추이 예측
5. 관광지 오디오가이드(odii)
6. (선택) 관광사진 정보

## env 키 이름 (SETUP에서 실제 이름 확정 · 값 출력 금지)
- KTO_SERVICE_KEY (또는 실제 발급 시 부여된 이름) — 이미 URL 인코딩된 값이면 재인코딩 금지.

## 지역 코드 (하드코딩 금지 · 코드조회 API로 기동 시 확보)
- 강원 areaCode / 춘천 sigunguCode → 코드조회 오퍼레이션으로 매칭·캐시(server/cache).

## 캐시·무결성
- kto_spots.raw = API 원문 JSON 그대로. TTL 24h. lazy 갱신.
- 성지 여부·K배지는 SOURCE_SPOTS.md 태그로만 판정. API 원문에 없는 성지 주장 금지.

## docs/kto/ 배치 목록 (README로도 안내)
위 1~6 서비스의 '오픈API 활용가이드' 문서를 docs/kto/ 에 넣는다. 파일명에 서비스명 포함.
